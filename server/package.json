{
  "name": "commodaflow-server",
  "version": "1.0.0",
  "description": "Backend para gestión de alquiler de hardware",
  "main": "src/index.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  }
}